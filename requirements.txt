# =============================================================================
#  Attendance system — Python dependencies
# =============================================================================

# --- Web server + dashboard + login portal (needed everywhere) ---
Flask>=3.0
flask-cors>=4.0
Werkzeug>=3.0
psycopg2-binary>=2.9
gunicorn>=21.0            # production WSGI server (Render uses this via Procfile)

# --- Face recognition (needed here too — admin panel enrollment can run
#     from Render now, not just the Jetson; face_embedding.py always uses
#     CPU-only inference so it works without a GPU) ---
numpy>=1.24
opencv-python-headless>=4.8   # no GUI bindings (imshow/etc) — nothing here
                              # needs them, and the full opencv-python build
                              # costs real memory we can't spare on a
                              # 512MB Render instance.
insightface>=0.7.3
onnxruntime>=1.16    # CPU-only package — Render has no GPU. The Jetson's
                     # camera processes use their own separately-managed
                     # conda env with onnxruntime-gpu; this file doesn't
                     # affect them.
