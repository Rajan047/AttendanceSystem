# =============================================================================
#  Attendance system — Python dependencies
# =============================================================================

# --- Web server + dashboard + login portal (needed everywhere) ---
Flask>=3.0
flask-cors>=4.0
Werkzeug>=3.0
psycopg2-binary>=2.9
gunicorn>=21.0            # production WSGI server (Render uses this via Procfile)

# --- Face recognition (ONLY needed on a machine that does photo enrollment
#     with enough memory for it, e.g. the Jetson — NOT installed here.
#     portal.py detects their absence (FACE_EMBEDDING_AVAILABLE) and skips
#     embedding generation gracefully; the dashboard/API/portal don't need
#     these at all. Tried running this stack on Render's 512MB free tier —
#     it OOM-crash-looped even after trimming to CPU-only + 2 of 5 model
#     modules + headless opencv. Not worth the memory on a host this size.
# numpy>=1.24
# opencv-python-headless>=4.8
# insightface>=0.7.3
# onnxruntime>=1.16
requests>=2.28
